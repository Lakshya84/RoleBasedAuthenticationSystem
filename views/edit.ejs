<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-screen h-screen bg-zinc-900 p-10 text-white text-lg">
        <div class="nav mb-8">
            <a href="/read" class="text-blue-500 text-sm border-b-2 inline-block border-blue-500">Read Users...</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-5 border-b-2 inline-block">Update User</h1>
        <form action="/update/<%= user._id%>" method="post">
            <input type="text" class="px-5 py-2 rounded-lg border-2 border-zinc-800 bg-transparent" placeholder="name"
                name="name" value="<%= user.name%>">
            <input type="email" class="px-5 py-2 rounded-lg border-2 border-zinc-800 bg-transparent" placeholder="email"
                name="email" value="<%= user.email%>">
            <input type="text" class="px-5 py-2 rounded-lg border-2 border-zinc-800 bg-transparent"
                placeholder="image url" name="image" value="<%= user.image%>">
            <input type="submit" value="Update a User"
                class="px-5 py-2 rounded-lg font-semibold border-2 text-black border-yellow-700 bg-yellow-500">
        </form>
    </div>
</body>

</html>